diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/Wallet.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/Wallet.js
index 608da7f..bcb1332 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/Wallet.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/Wallet.js
@@ -270,11 +270,18 @@ class Wallet {
         sdk_1.Validation.validateOriginator(originator);
         const { vargs } = this.validateAuthAndArgs(args, sdk_1.Validation.validateListActionsArgs);
         const r = await this.storage.listActions(vargs);
-        // Implement security policy to block customInstructions from output results.
-        for (const action of r.actions) {
-            if (action.outputs != null) {
-                for (const output of action.outputs) {
-                    output.customInstructions = undefined;
+        // Custom instructions are wallet-private metadata. The first-party
+        // Vault needs its own authenticated history in order to prove that a
+        // derived salt was never reused, but connected applications must keep
+        // seeing the upstream redacted shape.
+        const vaultAdmin = this.__bsvVaultAdminOriginator !== undefined &&
+            originator === this.__bsvVaultAdminOriginator;
+        if (!vaultAdmin) {
+            for (const action of r.actions) {
+                if (action.outputs != null) {
+                    for (const output of action.outputs) {
+                        output.customInstructions = undefined;
+                    }
                 }
             }
         }
@@ -593,6 +600,13 @@ class Wallet {
             }
             const { auth, vargs } = this.validateAuthAndArgs(args, sdk_1.Validation.validateCreateActionArgs, logger);
             logger?.log('validated args');
+            // BSV Browser Vault authorization is decided from the originator
+            // supplied by WalletClient, never from caller-controlled action
+            // fields. Carry that decision only inside the validated storage
+            // request so the provider can reject R1C inputs atomically at its
+            // authenticated source-script boundary.
+            vargs.__bsvVaultAdminAuthorized = this.__bsvVaultAdminOriginator !== undefined &&
+                originator === this.__bsvVaultAdminOriginator;
             vargs.includeAllSourceTransactions = this.includeAllSourceTransactions;
             if ((this.randomVals != null) && this.randomVals.length > 1) {
                 vargs.randomVals = [...this.randomVals];
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/WalletPermissionsManager.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/WalletPermissionsManager.js
index 17cbea2..2a21776 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/WalletPermissionsManager.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/WalletPermissionsManager.js
@@ -2996,7 +2996,12 @@ class WalletPermissionsManager {
         for (let i = 0; i < requested.length; i++) {
             const wantScript = (requested[i].lockingScript ?? '').toLowerCase();
             const wantSats = requested[i].satoshis;
-            const match = available.find(a => !a.used && a.script === wantScript && a.satoshis === wantSats);
+            // A sendMax output is requested as maxPossibleSatoshis (see
+            // generateChange.maxPossibleSatoshis); generateChange rewrites its
+            // amount to whatever the inputs can fund, so only the locking
+            // script can be pinned for that output.
+            const isSendMax = wantSats === 2099999999999999;
+            const match = available.find(a => !a.used && a.script === wantScript && (isSendMax || a.satoshis === wantSats));
             if (match == null) {
                 throw new Error(`The transaction returned for signing does not contain caller-requested output ${i} ` +
                     `(locking script and amount). The recipient may have been substituted by storage.`);
@@ -3021,13 +3026,23 @@ class WalletPermissionsManager {
                 });
             }
         }
-        // Sum originator-requested outputs:
-        const totalOutputSatoshis = (args.outputs || []).reduce((acc, out) => acc + out.satoshis, 0);
+        // Sum originator-requested outputs. A sendMax output is requested as
+        // maxPossibleSatoshis and rewritten by generateChange to the funded
+        // amount, so read its real figure off the transaction (matched by
+        // locking script) rather than off the request.
+        const actualOutputSats = (out) => {
+            if (out.satoshis !== 2099999999999999)
+                return out.satoshis;
+            const wantScript = (out.lockingScript ?? '').toLowerCase();
+            const match = tx.outputs.find(o => o.lockingScript.toHex().toLowerCase() === wantScript);
+            return match != null ? match.satoshis : out.satoshis;
+        };
+        const totalOutputSatoshis = (args.outputs || []).reduce((acc, out) => acc + actualOutputSats(out), 0);
         for (const outIndex in args.outputs || []) {
             const out = args.outputs[outIndex];
             lineItems.push({
                 type: 'output',
-                satoshis: out.satoshis,
+                satoshis: actualOutputSats(out),
                 description: originalOutputDescriptions[outIndex] || 'No output description provided'
             });
         }
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/buildSignableTransaction.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/buildSignableTransaction.js
index 8540a7c..e11a10e 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/buildSignableTransaction.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/buildSignableTransaction.js
@@ -188,7 +188,14 @@ function verifyRequestedOutputsUnchanged(storageOutputs, args) {
         if (requestedScript.length === 0 || requestedScript !== providedScript) {
             throw new WERR_errors_1.WERR_INVALID_PARAMETER('output.lockingScript', `equal to the caller-requested locking script. Storage returned a different locking script for output index ${i}; the recipient may have been substituted.`);
         }
-        if (provided.satoshis !== requested.satoshis) {
+        // A sendMax output is requested as maxPossibleSatoshis (see
+        // generateChange.maxPossibleSatoshis); generateChange rewrites its
+        // amount to whatever the inputs can fund, so only the locking
+        // script can be pinned for that output. Any rewrite is bounded by
+        // the wallet's own inputs, and diverting the difference to change
+        // still pays this client.
+        const isSendMax = requested.satoshis === 2099999999999999;
+        if (!isSendMax && provided.satoshis !== requested.satoshis) {
             throw new WERR_errors_1.WERR_INVALID_PARAMETER('output.satoshis', `equal to the caller-requested satoshis. Storage returned ${provided.satoshis} for output index ${i}, caller requested ${requested.satoshis}.`);
         }
     }
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/completeSignedTransaction.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/completeSignedTransaction.js
index 4569453..032f38c 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/completeSignedTransaction.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/signer/methods/completeSignedTransaction.js
@@ -28,20 +28,85 @@ async function completeSignedTransaction(prior, spends, wallet) {
     /// //////////////////
     // Insert SABPPP unlock templates for wallet signed inputs
     /// //////////////////
-    for (const pdi of prior.pdi) {
-        const sabppp = new ScriptTemplateBRC29_1.ScriptTemplateBRC29({
-            derivationPrefix: pdi.derivationPrefix,
-            derivationSuffix: pdi.derivationSuffix,
-            keyDeriver: wallet.keyDeriver
-        });
-        const keys = wallet.getClientChangeKeyPair();
-        const lockerPrivKey = keys.privateKey;
-        const unlockerPubKey = pdi.unlockerPubKey || keys.publicKey;
-        const sourceSatoshis = pdi.sourceSatoshis;
-        const lockingScript = (0, utilityHelpers_1.asBsvSdkScript)(pdi.lockingScript);
-        const unlockTemplate = sabppp.unlock(lockerPrivKey, unlockerPubKey, sourceSatoshis, lockingScript);
-        const input = prior.tx.inputs[pdi.vin];
-        input.unlockingScriptTemplate = unlockTemplate;
+    // BSV Browser patch (native-crypto M3, issue #9): batch the per-input BRC-42
+    // child-key derivations into ONE async native crossing per counterparty
+    // (batchBrc42DeriveChild — an off-JS-thread Nitro method; the ECDH shared
+    // secret is computed once per counterparty instead of once per input).
+    // Taken ONLY when the native module is present AND wallet.keyDeriver is the
+    // plain callback-free KeyDeriver whose rootKey matches the client change key
+    // — exactly the case where ScriptTemplateBRC29.getKeyDeriver would reuse
+    // wallet.keyDeriver. Derived keys are byte-identical to derivePrivateKey
+    // (bsv-fuzz batch_derive class + vectors, 0 mismatches); the resulting
+    // P2PKH unlock template construction is the same call ScriptTemplateBRC29
+    // makes. Any failure falls back to the original per-input loop below.
+    let nativeBatchedPdi = false;
+    if (prior.pdi.length > 0) {
+        try {
+            const nat = globalThis.__bsvSecpNative;
+            const kd = wallet.keyDeriver;
+            const keys = wallet.getClientChangeKeyPair();
+            let rootPriv = keys.privateKey;
+            if (typeof rootPriv === 'string') {
+                rootPriv = sdk_1.PrivateKey.fromHex(rootPriv);
+            }
+            if (nat != null && typeof nat.batchBrc42DeriveChild === 'function' &&
+                kd != null && kd.cacheSharedSecret === undefined && kd.retrieveCachedSharedSecret === undefined &&
+                typeof kd.computeInvoiceNumber === 'function' && typeof kd.normalizeCounterparty === 'function' &&
+                kd.rootKey != null && typeof kd.rootKey.toHex === 'function' &&
+                kd.rootKey.toHex() === rootPriv.toHex()) {
+                // Group inputs by counterparty pubkey; invoice numbers vary per input.
+                const groups = new Map();
+                for (const pdi of prior.pdi) {
+                    const unlockerPubKey = pdi.unlockerPubKey || keys.publicKey;
+                    const keyID = `${pdi.derivationPrefix ?? ''} ${pdi.derivationSuffix ?? ''}`;
+                    const invoiceNumber = kd.computeInvoiceNumber(ScriptTemplateBRC29_1.brc29ProtocolID, keyID);
+                    const cp = kd.normalizeCounterparty(unlockerPubKey);
+                    const cpHex = cp.encode(true, 'hex');
+                    if (!groups.has(cpHex)) {
+                        groups.set(cpHex, { cp, pdis: [], invoices: [] });
+                    }
+                    const g = groups.get(cpHex);
+                    g.pdis.push(pdi);
+                    g.invoices.push(invoiceNumber);
+                }
+                const root32 = Uint8Array.from(kd.rootKey.toArray('be', 32)).buffer;
+                const p2pkh = new sdk_1.P2PKH();
+                for (const g of groups.values()) {
+                    const cp33 = Uint8Array.from(g.cp.encode(true)).buffer;
+                    const childCat = new Uint8Array(await nat.batchBrc42DeriveChild(root32, cp33, g.invoices));
+                    if (childCat.length !== g.pdis.length * 32) {
+                        throw new Error('batchBrc42DeriveChild length mismatch');
+                    }
+                    for (let j = 0; j < g.pdis.length; j++) {
+                        const pdi = g.pdis[j];
+                        const derived = new sdk_1.PrivateKey(Array.from(childCat.subarray(j * 32, (j + 1) * 32)));
+                        const unlockTemplate = p2pkh.unlock(derived, 'all', false, pdi.sourceSatoshis, (0, utilityHelpers_1.asBsvSdkScript)(pdi.lockingScript));
+                        prior.tx.inputs[pdi.vin].unlockingScriptTemplate = unlockTemplate;
+                    }
+                }
+                nativeBatchedPdi = true;
+            }
+        }
+        catch (e) {
+            nativeBatchedPdi = false;
+        }
+    }
+    if (!nativeBatchedPdi) {
+        for (const pdi of prior.pdi) {
+            const sabppp = new ScriptTemplateBRC29_1.ScriptTemplateBRC29({
+                derivationPrefix: pdi.derivationPrefix,
+                derivationSuffix: pdi.derivationSuffix,
+                keyDeriver: wallet.keyDeriver
+            });
+            const keys = wallet.getClientChangeKeyPair();
+            const lockerPrivKey = keys.privateKey;
+            const unlockerPubKey = pdi.unlockerPubKey || keys.publicKey;
+            const sourceSatoshis = pdi.sourceSatoshis;
+            const lockingScript = (0, utilityHelpers_1.asBsvSdkScript)(pdi.lockingScript);
+            const unlockTemplate = sabppp.unlock(lockerPrivKey, unlockerPubKey, sourceSatoshis, lockingScript);
+            const input = prior.tx.inputs[pdi.vin];
+            input.unlockingScriptTemplate = unlockTemplate;
+        }
     }
     /// //////////////////
     // Sign wallet signed inputs making transaction fully valid.
@@ -102,5 +167,114 @@ function verifyUnlockScripts(txid, beef) {
             throw new WERR_errors_1.WERR_INVALID_PARAMETER(`inputs[${i}].unlockScript`, `valid. ${e.message}`);
         }
     }
+    // BSV Browser patch (native-crypto M5.6, issue #24): SHADOW-MODE Tier-3
+    // engine verify. The JS `Spend` loop above stayed AUTHORITATIVE (reaching
+    // here means every input verified valid). Now re-verify the whole tx through
+    // the native engine's ONE `batchVerifyP2pkhInputs` crossing ALONGSIDE the JS
+    // result and log any divergence LOUDLY — but never change the verdict this
+    // rung (shadow = observe; a later rung flips authority once shadow is clean).
+    // P2PKH-eligible only; any ineligible input skips the shadow (identical
+    // philosophy to the M5.5 sign routing).
+    engineShadowVerify(txid, tx);
+}
+/**
+ * Shadow-mode native verify (issue #24). Fire-and-(mostly)-forget: kicks the
+ * engine's async `batchVerifyP2pkhInputs` and compares its per-input verdict
+ * bitmap to the JS `Spend` result (which, at this call site, is all-valid).
+ * Divergences → `console.error` with tx/input detail + a global counter the
+ * proof harness reads. The returned/attached promise is stashed on
+ * `globalThis.__bsvEngineShadow.pending` so tests can await the comparison.
+ */
+function engineShadowVerify(txid, tx) {
+    const shadow = (globalThis.__bsvEngineShadow ??= {
+        verifyChecks: 0, verifyEligible: 0, verifySkipped: 0,
+        verifyDivergences: 0, verifyAgreeInputs: 0, pending: Promise.resolve(),
+        lastDivergence: null
+    });
+    shadow.verifyChecks++;
+    try {
+        const eng = globalThis.__bsvEngineNative;
+        if (eng == null || typeof eng.batchVerifyP2pkhInputs !== 'function') {
+            shadow.verifySkipped++;
+            return; // no engine (web/jest/Expo Go) — nothing to shadow
+        }
+        const SIGHASH_FORKID = 0x40;
+        const SIGHASH_CHRONICLE = 0x20;
+        const n = tx.inputs.length;
+        if (n === 0) { shadow.verifySkipped++; return; }
+        // Build the signed-tx bytes + 37B prevout meta; bail (skip) on the FIRST
+        // ineligible input — the engine only handles canonical P2PKH FORKID.
+        const metaBytes = new Uint8Array(n * 37);
+        const mv = new DataView(metaBytes.buffer);
+        for (let i = 0; i < n; i++) {
+            const input = tx.inputs[i];
+            const sourceOutput = input.sourceTransaction?.outputs?.[input.sourceOutputIndex];
+            if (sourceOutput == null) { shadow.verifySkipped++; return; }
+            const lock = sourceOutput.lockingScript.toBinary();
+            // 25-byte P2PKH lock shape.
+            if (lock.length !== 25 || lock[0] !== 0x76 || lock[1] !== 0xa9 ||
+                lock[2] !== 0x14 || lock[23] !== 0x88 || lock[24] !== 0xac) {
+                shadow.verifySkipped++; return;
+            }
+            const unlock = input.unlockingScript?.toBinary();
+            // canonical two-push shape: push(sig‖scope) push(pubkey33).
+            if (unlock == null || unlock.length < 2) { shadow.verifySkipped++; return; }
+            const l1 = unlock[0];
+            if (l1 < 2 || l1 >= 0x4c || unlock.length !== 1 + l1 + 1 + 33 ||
+                unlock[1 + l1] !== 33) {
+                shadow.verifySkipped++; return;
+            }
+            const scopeByte = unlock[l1]; // last byte of the sig element
+            if ((scopeByte & SIGHASH_FORKID) === 0 || (scopeByte & SIGHASH_CHRONICLE) !== 0) {
+                shadow.verifySkipped++; return;
+            }
+            const sats = sourceOutput.satoshis ?? 0;
+            if (!Number.isInteger(sats) || sats < 0 || sats > Number.MAX_SAFE_INTEGER) {
+                shadow.verifySkipped++; return;
+            }
+            mv.setUint32(i * 37, i, true);
+            mv.setUint32(i * 37 + 4, sats >>> 0, true);
+            mv.setUint32(i * 37 + 8, Math.floor(sats / 0x100000000), true);
+            metaBytes.set(lock, i * 37 + 12);
+        }
+        shadow.verifyEligible++;
+        const signedBuf = Uint8Array.from(tx.toBinary()).buffer;
+        const metaBuf = metaBytes.buffer;
+        shadow.pending = (async () => {
+            try {
+                const bitmap = new Uint8Array(await eng.batchVerifyP2pkhInputs(signedBuf, metaBuf));
+                // JS verdict at this call site = all-valid. Any 0 (or wrong
+                // length) is a divergence.
+                let diverged = false;
+                if (bitmap.length !== n) {
+                    diverged = true;
+                }
+                else {
+                    for (let i = 0; i < n; i++) {
+                        if (bitmap[i] === 1) { shadow.verifyAgreeInputs++; }
+                        else {
+                            diverged = true;
+                            console.error(`[EngineShadow] VERIFY DIVERGENCE txid=${txid} input=${i}: engine=INVALID, JS=VALID`);
+                        }
+                    }
+                }
+                if (diverged) {
+                    shadow.verifyDivergences++;
+                    shadow.lastDivergence = { txid, bitmap: Array.from(bitmap), n };
+                }
+            }
+            catch (e) {
+                // Engine Err on an ELIGIBLE, JS-valid tx is itself a divergence
+                // (JS said valid; engine could not confirm) — log loudly.
+                shadow.verifyDivergences++;
+                shadow.lastDivergence = { txid, error: String(e), n };
+                console.error(`[EngineShadow] VERIFY DIVERGENCE txid=${txid}: engine rejected an eligible JS-valid tx: ${String(e)}`);
+            }
+        })();
+    }
+    catch (e) {
+        // Shadow must NEVER affect the authoritative path.
+        shadow.verifySkipped++;
+    }
 }
 //# sourceMappingURL=completeSignedTransaction.js.map
\ No newline at end of file
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/StorageProvider.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/StorageProvider.js
index c9b8de6..c234104 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/StorageProvider.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/StorageProvider.js
@@ -564,8 +564,24 @@ class StorageProvider extends StorageReaderWriter_1.StorageReaderWriter {
     async relinquishOutput(auth, args) {
         const vargs = sdk_1.Validation.validateRelinquishOutputArgs(args);
         const { txid, vout } = sdk_1.Validation.parseWalletOutpoint(vargs.output);
-        const output = (0, utilityHelpers_1.verifyOne)(await this.findOutputs({ partial: { userId: auth.userId, txid, vout } }));
-        return await this.updateOutput(output.outputId, { basketId: undefined });
+        return await this.transaction(async (trx) => {
+            const output = (0, utilityHelpers_1.verifyOne)(await this.findOutputs({
+                partial: { userId: auth.userId, txid, vout },
+                trx
+            }));
+            const basket = (0, utilityHelpers_1.verifyOne)(await this.findOutputBaskets({
+                partial: { userId: auth.userId, name: vargs.basket },
+                trx
+            }));
+            // The basket in the request is an authorization claim, not the
+            // identity of the stored output. Verify it against storage before
+            // removing anything; otherwise access to any ordinary basket can
+            // be used to relinquish an admin-basket output by outpoint.
+            if (output.basketId !== basket.basketId) {
+                throw new WERR_errors_1.WERR_INVALID_PARAMETER('basket', 'the basket that currently contains the output');
+            }
+            return await this.updateOutput(output.outputId, { basketId: undefined }, trx);
+        });
     }
     async processSyncChunk(args, chunk) {
         const user = (0, utilityHelpers_1.verifyTruthy)(await this.findUserByIdentityKey(args.identityKey));
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/createAction.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/createAction.js
index ab8101e..f1ee46b 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/createAction.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/createAction.js
@@ -565,6 +565,12 @@ async function resolveInputScript(storage, userId, vargs, input, beef, preloaded
         input.satoshis = sdk_1.Validation.validateSatoshis(so.satoshis, 'so.satoshis');
         input.lockingScript = so.lockingScript;
     }
+    // Optional host policy hook. It runs only after the source output has been
+    // resolved from authenticated local storage or verified BEEF, so a caller
+    // cannot lie about the locking script to bypass a protected-input rule.
+    if (typeof storage.validateResolvedActionInput === 'function') {
+        await storage.validateResolvedActionInput(vargs, input);
+    }
 }
 async function validateNoSendChange(storage, userId, vargs, changeBasket) {
     const r = [];
@@ -629,7 +635,14 @@ async function fundNewTransactionSdk(storage, userId, vargs, ctx) {
         changeFirstSatoshis: Math.max(1, Math.round(ctx.changeBasket.minimumDesiredUTXOValue / 4)),
         changeLockingScriptLength: 25,
         changeUnlockingScriptLength: 107,
-        targetNetCount: ctx.changeBasket.numberOfDesiredUTXOs - ctx.availableChangeCount,
+        // PATCH(bsv-browser): keep a Vault deposit's transaction plan minimal.
+        // UTXO-pool growth (targetNetCount) would add funding inputs and split
+        // unrelated change into a transaction carrying the large R1C output,
+        // increasing later BEEF and review costs. The Vault path validates the
+        // complete signed plan before releasing it from noSend.
+        targetNetCount: (vargs.labels ?? []).includes('vault-deposit')
+            ? undefined
+            : ctx.changeBasket.numberOfDesiredUTXOs - ctx.availableChangeCount,
         randomVals: vargs.randomVals
     };
     const noSendChange = [...ctx.noSendChangeIn];
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.d.ts b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.d.ts
index 9d934ee..0affefe 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.d.ts
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.d.ts
@@ -89,14 +89,14 @@ export declare function restoreInputsToSpendable(storage: StorageProvider, trans
  *
  * "basket insertion" Merge Rules:
  * 1. The "default" basket may not be specified as the insertion basket.
- * 2. Managed change may not be reclassified as a basket insertion.
+ * 2. An existing output may only be internalized idempotently into its current
+ *    basket. Basket insertion is not an output-reclassification API.
  * 3. Basket insertions do not affect wallet balance and are typed "custom".
  *
  * "wallet payment" Merge Rules:
  * 1. Targeting an existing managed output is idempotent.
- * 2. Targeting an existing custom output converts it to managed BRC-29
- *    change and increases wallet balance. This includes verified recovery of
- *    a legacy custom row that was incorrectly placed in the default basket.
+ * 2. Targeting a custom output converts it to managed BRC-29 change after
+ *    the signer layer verifies its exact locking script.
  */
 export declare function internalizeAction(storage: StorageProvider, auth: AuthId, args: InternalizeActionArgs): Promise<StorageInternalizeActionResult>;
 //# sourceMappingURL=internalizeAction.d.ts.map
diff --git a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.js b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.js
index 6a70185..5898de2 100644
--- a/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.js
+++ b/node_modules/@bsv/wallet-toolbox-mobile/out/src/storage/methods/internalizeAction.js
@@ -127,14 +127,14 @@ async function restoreInputsToSpendable(storage, transitions) {
  *
  * "basket insertion" Merge Rules:
  * 1. The "default" basket may not be specified as the insertion basket.
- * 2. Managed change may not be reclassified as a basket insertion.
+ * 2. An existing output may only be internalized idempotently into its current
+ *    basket. Basket insertion is not an output-reclassification API.
  * 3. Basket insertions do not affect wallet balance and are typed "custom".
  *
  * "wallet payment" Merge Rules:
  * 1. Targeting an existing managed output is idempotent.
- * 2. Targeting an existing custom output converts it to managed BRC-29
- *    change and increases wallet balance. This includes verified recovery of
- *    a legacy custom row that was incorrectly placed in the default basket.
+ * 2. Targeting a custom output converts it to managed BRC-29 change after
+ *    the signer layer verifies its exact locking script.
  */
 async function internalizeAction(storage, auth, args) {
     const ctx = new InternalizeActionContext(storage, auth, args);
@@ -289,9 +289,12 @@ class InternalizeActionContext {
                 if ((0, managedChange_1.isManagedChangeOutput)(basket.eo)) {
                     throw new WERR_errors_1.WERR_INVALID_PARAMETER('outputs', `output ${basket.vout} is wallet-managed change and cannot be reclassified as a basket insertion`);
                 }
-                // An incompatible legacy row in the default basket may be moved to a
-                // non-default recovery basket. It was not part of managed balance, so
-                // this metadata repair has no satoshi adjustment.
+                const currentBasket = (0, utilityHelpers_1.verifyOne)(await this.storage.findOutputBaskets({
+                    partial: { userId: this.userId, basketId: basket.eo.basketId }
+                }));
+                if (currentBasket.name !== basket.basket) {
+                    throw new WERR_errors_1.WERR_INVALID_PARAMETER('outputs', `output ${basket.vout} is already assigned to basket ${currentBasket.name} and cannot be reclassified`);
+                }
             }
         }
         for (const payment of this.walletPayments) {
@@ -302,9 +305,9 @@ class InternalizeActionContext {
                         payment.ignore = true;
                     }
                     else {
-                        // Verified signer processing established that this is BRC-29.
-                        // Promote a custom row (including legacy custom-in-default rows)
-                        // to managed change and add it to balance.
+                        // The signer layer verified this exact output as BRC-29.
+                        // Promote the custom row to managed change and add it
+                        // to balance; an R1C Vault lock cannot pass that check.
                         this.satoshis += payment.txo.satoshis;
                     }
                 }
